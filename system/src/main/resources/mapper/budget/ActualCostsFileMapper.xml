<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.cmkj.budget.mapper.ActualCostsFileMapper">

    <resultMap type="ActualCostsFile" id="ActualCostsFileResult">
        <result property="id"    column="id"    />
        <result property="sourceName"    column="source_name"    />
        <result property="fileName"    column="file_name"    />
        <result property="filePath"    column="file_path"    />
        <result property="infoNum"    column="info_num"    />
        <result property="createBy"    column="create_by"    />
        <result property="createTime"    column="create_time"    />
        <result property="delFlag"    column="del_flag"    />
    </resultMap>

    <sql id="selectActualCostsFileVo">
        select id, source_name, file_name, file_path, info_num, create_by, create_time, del_flag from t_actual_costs_file
    </sql>

    <select id="selectActualCostsFileList" parameterType="ActualCostsFile" resultMap="ActualCostsFileResult">
        <include refid="selectActualCostsFileVo"/>
         where del_flag = '0'
        <if test="sourceName != null  and sourceName != ''"> and source_name like concat('%', #{sourceName}, '%')</if>
        <if test="fileName != null  and fileName != ''"> and file_name like concat('%', #{fileName}, '%')</if>
        <if test="infoNum != null "> and info_num = #{infoNum}</if>
        <if test="createTime != null "> AND DATE(create_time) = DATE(#{createTime})</if>
         order by create_time desc
    </select>

    <select id="selectActualCostsFileById" parameterType="Long" resultMap="ActualCostsFileResult">
        <include refid="selectActualCostsFileVo"/>
        where id = #{id}
    </select>

    <insert id="insertActualCostsFile" parameterType="ActualCostsFile" useGeneratedKeys="true" keyProperty="id">
        insert into t_actual_costs_file
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="sourceName != null">source_name,</if>
            <if test="fileName != null">file_name,</if>
            <if test="filePath != null">file_path,</if>
            <if test="infoNum != null">info_num,</if>
            <if test="createBy != null">create_by,</if>
            <if test="createTime != null">create_time,</if>
            <if test="delFlag != null and delFlag != ''">del_flag,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="sourceName != null">#{sourceName},</if>
            <if test="fileName != null">#{fileName},</if>
            <if test="filePath != null">#{filePath},</if>
            <if test="infoNum != null">#{infoNum},</if>
            <if test="createBy != null">#{createBy},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="delFlag != null and delFlag != ''">#{delFlag},</if>
        </trim>
    </insert>

    <update id="updateActualCostsFile" parameterType="ActualCostsFile">
        update t_actual_costs_file
        <trim prefix="SET" suffixOverrides=",">
            <if test="infoNum != null">info_num = #{infoNum},</if>
            <if test="delFlag != null and delFlag != ''">del_flag = #{delFlag},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteActualCostsFileById">
        update t_actual_costs_file
        set del_flag = '2'   where id = #{id}
    </delete>

    <delete id="deleteActualCostsFileByIds">
        update t_actual_costs_file
        set del_flag = '2'  where id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>
